# AWS Comet Assistant Prompts
*(Copy and paste these strings sequentially into your Comet Assistant or AWS Support Chatbot to have it securely guide you through building the exact architectural systems natively via the console).*

### Step 1: ECR Configuration
**Prompt 1:**
> "I have a PyTorch machine learning OCR Docker Image on my local Windows machine that I want to push. I need to create an Amazon ECR Private Repository named 'medical-ocr-api'. Please guide me step-by-step inside the Console on how to create this repo. Once generated, please output the exact `docker login`, `docker tag`, and `docker push` shell commands I need to execute organically in my powershell."

### Step 2: Lambda Foundation Setup
**Prompt 2:**
> "I successfully pushed the Docker container up to ECR! Now, I need to create an AWS Lambda deployment that triggers organically from this Container Image instead of a standard zip file. Please guide me meticulously via the Console GUI on securely deploying a new Lambda function completely hooked up to my 'medical-ocr-api' container tag inside the Elastic Container Registry."

### Step 3: Critical Memory Overrides
**Prompt 3:**
> "The Lambda script dynamically imports massive PyTorch neural blocks, so the mathematical execution is extremely heavy. Please tell me exactly how to navigate into Lambda's 'General Configuration' tab sequentially to mathematically increase the Memory Allocation to 2048 MB, and extend the maximum mathematical execution Timeout barrier sequentially to 1 minute."

### Step 4: Finalizing Public API Bridges
**Prompt 4:**
> "My Docker Lambda architecture is securely hooked up! Finally, I structurally need to invoke this code remotely from my phone or web-browser organically, and the code inherently leverages FastAPI natively via the Mangum handler block. How do I dynamically enable a public 'Function URL' securely bypassing internal IAM auth, and how do I dynamically configure CORS so my web browsers won't aggressively block the neural payload requests?"
